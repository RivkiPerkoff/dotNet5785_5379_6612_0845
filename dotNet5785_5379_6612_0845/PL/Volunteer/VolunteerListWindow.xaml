<Window x:Class="PL.Volunteer.VolunteerListWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
       xmlns:local="clr-namespace:PL.Volunteer"
         xmlns:pl="clr-namespace:PL"
        mc:Ignorable="d"
        Title="VolunteerListWindow" Height="640" Width="800"
        Loaded="Window_Loaded"
        Closed="Window_Closed"
        DataContext="{Binding RelativeSource={RelativeSource Self}}">

    <Window.Resources>
        <pl:VolunteerFieldsCollection x:Key="VolunteerFieldsCollectionKey"/>
    </Window.Resources>


    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- ComboBox למיון -->
        <Grid Grid.Row="0" Margin="10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Label Grid.Column="0" Content="Select:" VerticalAlignment="Center" Margin="10"/>
            <ComboBox Grid.Column="1" Margin="10"
                ItemsSource="{Binding Source={StaticResource VolunteerFieldsCollectionKey}}"
                SelectedValue="{Binding SelectedSortField, Mode=TwoWay}">
                <ComboBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding}" />
                    </DataTemplate>
                </ComboBox.ItemTemplate>
            </ComboBox>

        </Grid>
        <!-- 
        <DataGrid Grid.Row="1"
          ItemsSource="{Binding VolunteerList}"
          SelectedItem="{Binding SelectedVolunteer, Mode=OneWayToSource}"
          AutoGenerateColumns="True"
          Margin="10"
         />טבלת מתנדבים -->
        <!--    MouseDoubleClick="VolunteerDataGrid_MouseDoubleClick"-->
        <DataGrid Grid.Row="1"
          ItemsSource="{Binding VolunteerList}"
          SelectedItem="{Binding SelectedVolunteer, Mode=OneWayToSource}"
          AutoGenerateColumns="False"
          Margin="10"
          CanUserAddRows="False"
          >
            <DataGrid.Columns>
                <!-- עמודות קיימות - נניח פשוט Name ו-VolunteerId לדוגמה -->
                <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*"/>
                <DataGridTextColumn Header="Id" Binding="{Binding VolunteerId}" Width="100"/>

                <!-- עמודת כפתור מחיקה -->
                <DataGridTemplateColumn Header="Delete" Width="80">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="Delete" Click="DeleteButton_Click" Tag="{Binding VolunteerId}" />
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>


        <!-- כפתור הוספה -->
        <Button Grid.Row="2" Content="Add" Height="50" Width="100" Margin="10"
                HorizontalAlignment="Center"
            />
        <!--   Click="AddButton_Click"-->

    </Grid>
</Window>
